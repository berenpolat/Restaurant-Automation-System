package gui;

import javax.swing.*;

public class ViewMenu extends JFrame {
    public ViewMenu() {
        setTitle("View Menu");
        setSize(400, 300);
        setDefaultCloseOperation(EXIT_ON_CLOSE);
    }
}