package gui;

import javax.swing.*;

public class OrderScreen extends JFrame {
    public OrderScreen() {
        setTitle("Create Order");
        setSize(400, 300);
        setDefaultCloseOperation(EXIT_ON_CLOSE);
    }
}